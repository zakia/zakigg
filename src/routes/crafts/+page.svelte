<script>
	let { data } = $props();
</script>

<div class="layout">
  <h1>Coming Soon...</h1>
	<!-- {#each data.posts as post}
		<a href={`/crafts/${post.slug}`}>
			{post.title}
			{#if post.date}
				<p>{post.date} {new Date(post.date)}</p>
			{/if}
		</a>
	{/each} -->
</div>
